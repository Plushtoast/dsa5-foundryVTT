<div>
  <div class='hideData row-section'>
    <div class='col ninety'>
      <b>{{item.name}}{{#if item.parent}} ({{item.parent.name}}){{/if}}</b>
    </div>
    <div class='col ten tinyEntityLink'>
      {{item.link}}
    </div>
  </div>
  <p>{{localize 'MAGICANALYSIS.requestIntro'}}</p>
  <div class="flexrow">
    <button class='startMagicAnalysis' data-uuid='{{item.uuid}}'>{{localize 'MAGICANALYSIS.start'}}</button>
  </div>
</div>
