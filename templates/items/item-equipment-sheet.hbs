<div class="tab standard-form scrollable {{tabs.details.cssClass}}" data-tab="details" data-group="{{tabs.details.group}}" class="tab">

    {{formGroup systemFields.equipmentType.fields.value value=document.system.equipmentType.value localize=true}}

    <div class="form-group double">
        <label>{{localize "structure"}}</label>
        <div class="form-fields">
            {{formInput systemFields.structure.fields.value value=document.system.structure.value}}
            <span> / </span>
            {{formInput systemFields.structure.fields.max value=document.system.structure.max}}
        </div>
    </div>

    {{formGroup systemFields.worn.fields.wearable value=document.system.worn.wearable localize=true}}

    {{#if (eq document.system.equipmentType.value 'bags')}}
    {{formGroup systemFields.capacity value=document.system.capacity localize=true}}
    {{/if}}

    {{formGroup systemFields.effect.fields.value value=document.system.effect.value localize=true}}

    <div class='form-group autoHeight'>
        <label>{{localize 'domains'}}</label>
        <div class='form-fields'>
            {{#if domains}}
                <div class='domainsPretty'>
                <ul>{{{domains}}}</ul>
                </div>
            {{/if}}
            <input
                class='{{ifThen domains "domainToggle"}}'
                placeholder='{{localize "WEAPON.clerical"}}, {{localize "WEAPON.magical"}}, {{localize "WEAPON.silverPlated"}}'
                type='text'
                name='system.effect.attributes'
                value='{{document.system.effect.attributes}}'
            />
        </div>
    </div>
    
    {{formGroup systemFields.region value=document.system.region localize=true}}
    {{> systems/dsa5/templates/items/traditionArtifact.hbs}}
</div>