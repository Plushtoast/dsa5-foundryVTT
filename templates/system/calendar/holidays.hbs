<div class='tab scrollable {{tabs.events.cssClass}}' data-tab='events' data-group='{{tabs.events.group}}'>
    <div class="fullSceenMaxWidth">
        <ul class="dsalist">
            {{#each holidays}}
            <li><b>{{day}} {{month}}</b>: {{name}}</li>
            {{/each}}
        </ul>
    </div>
</div>