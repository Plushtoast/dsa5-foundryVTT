<div class='tab scrollable {{tabs.holidays.cssClass}}' data-tab='holidays' data-group='{{tabs.holidays.group}}'>
    <ul class="dsalist">
        {{#each holidays}}
        <li><b>{{day}} {{month}}</b>: {{name}}</li>
        {{/each}}
    </ul>
</div>