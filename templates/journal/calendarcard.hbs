<article class="event-card" aria-labelledby="event-{{@key}}-title" data-category="{{category}}">

    <div class="event-card__date" aria-hidden="true" style="--gradient-color:{{color}}" data-tooltip="{{categoryName}}">
        <div class="month">{{monthShort}}</div>
        <div class="day">{{from.dayOfMonth}}</div>
        <div class="weekday" data-tooltip="{{weekDayLong}}">{{weekdayShort}}</div>
    </div>

    <div class="event-card__body">
        <div class="flexrow">
            <h3 id="event-{{@key}}-title" class="event-card__title">{{title}} {{#unless visible}}<i class="fas fa-eye-slash"
                data-tooltip="dsacalendar.FIELDS.calendarentries.visible.label"></i>{{/unless}}</h3>
            {{#if isOwner}}
            <div class="event-card-controls flex0">
                <button type="button" class="smallBtn fa-solid fa-edit icon" data-action="editEvent" data-uuid="{{uuid}}" data-key="{{calendarKey}}"></button>
            </div>
            {{/if}}
        </div>        

        <div class="event-card__meta">
            <span class="event-chip">
                <i class="far fa-clock"></i>
                <span>{{from.dayOfMonth}}.</span>
                {{#if to.dayOfMonth}}
                <span aria-hidden="true">–</span>
                <span>{{to.dayOfMonth}}.</span>
                {{/if}}
                <span>{{from.monthLong}} {{from.year}} {{yearSuffix}}</span>
            </span>

            {{#if location}}
            <span class="event-chip">
                <i class="fas fa-location-pin"></i>
                <span class="eventlocation">{{location}}</span> 
            </span>
            {{/if}}

            {{#if gods}}
            <span class="event-chip" data-tooltip="dsacalendar.gods">
                {{gods}}
            </span>
            {{/if}}

            {{#if recurring}}
            <span class="event-chip">
                <span><i class="fas fa-redo" data-tooltip="dsacalendar.FIELDS.calendarentries.recurring.label"></i></span>
            </span>
            {{/if}}
            
        </div>

        {{#if content}}
        <div class="event-card__desc">
            {{content}}
        </div>
        {{/if}}
    </div>    

</article>