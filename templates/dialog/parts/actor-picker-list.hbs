<div class='query-actor-picker' data-selection-mode='{{selectionMode}}'{{#if groupActorId}} data-group-actor-id='{{groupActorId}}'{{/if}}>
  <ul class='dsa-card-list query-actor-list scrollable'>
    {{#each actors as |actor|}}
      {{> systems/dsa5/templates/dialog/parts/actor-picker-row.hbs actor=actor selectionMode=../selectionMode}}
    {{/each}}
  </ul>
</div>
