<form class="standard-form">
    <div class="hint">{{{localize "Book.customBookHelp"}}}</div>
    {{formGroup systemFields.title value=document.system.title name='title' rootId=rootId localize=true}}
    {{formGroup systemFields.journal value=document.system.journal name='journal' rootId=rootId localize=true}}
    {{formGroup systemFields.splash value=document.system.splash name='splash' rootId=rootId localize=true}}
    {{formGroup systemFields.moduleName value=document.system.moduleName name='moduleName' rootId=rootId localize=true}}
</form>
