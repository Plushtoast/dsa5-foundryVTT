<fieldset class="open{{name}} breedcard tableOdd row-section position-relative">
    <legend>{{localize title}}</legend>
    <div class="row-section ">
        <div class="col fourty center">
            <img style="max-height:100px" src="{{icon}}"/>
        </div>
        <div class="col sixty flexcol">
            <p>{{localize desc}}</p>            
        </div>
    </div>
</fieldset>